{
  "extends": "react-app",
  "rules": {
    // Other rules...
    "react-hooks/exhaustive-deps": "warn",  // Change "error" to "warn"
    "no-unused-vars": "warn",               // Change "error" to "warn"
    "eqeqeq": "warn"                        // Change "error" to "warn"
  }
}
